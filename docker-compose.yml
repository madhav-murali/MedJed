services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
  api:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - redis

volumes:
  redis_data:
